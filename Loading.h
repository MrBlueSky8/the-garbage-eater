#pragma once
#include "Base.h"
#include "iostream"
class Loading :    public Base
{
public:
	Loading();
	Loading(int px, int py, int pw, int ph);
	~Loading();
	//void Dibujar_desdeImagen(Graphics^ g, Bitmap^ bmp);
};

